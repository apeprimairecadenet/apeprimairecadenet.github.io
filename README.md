# Site de l'APE Mélina Mercouri, Cadenet

HTML pur, sans build. Une page par dossier (`index.html`), un seul `style.css`. Publié par GitHub Pages depuis la branche `main`.

Pour modifier : éditer le fichier HTML, mettre à jour la date « Mis à jour le … » en pied de page, commiter, pousser.
